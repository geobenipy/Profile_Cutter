This small Python script takes the single .dat file from Kingdom and creates new .dat files from it, one for each profile, which can then be loaded into QGIS.
Thus, only a single file now needs to be exported from Kingdom, saving the effort of exporting each profile individually.

You're welcome
Made by Beni